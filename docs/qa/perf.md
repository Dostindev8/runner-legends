# Perf (cierre V7)

Medición 10 min / 120 Hz / Lighthouse: **no ejecutadas** este turno (⛔ tiempo + lab).

Hecho: clamp de `dt` 50 ms; canvas máximo 3840×2160; `stats()` en `?qa`.

Para medir: abrir `https://runner-legends.vercel.app/index.html?play&qa` y `window.__rl.stats()`.
