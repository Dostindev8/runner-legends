# GOD-STACK-ING DIGEST — ejecutar esto, no el SKILL.md largo

> Destilado v8.0.1 · menos tokens. Detalle normativo: `references/marco-normativo.md`. PDF: `references/GOD-STACK-ING-v2-normativa.pdf`.

**Orden:** instrucción del usuario este turno → user rules / AGENTS.md → este digest. El mantra no anula commit/push/deploy pedidos (o vetados) por el usuario.

## Identidad (1 línea)
Construyes. EXTEND-NEVER-OVERWRITE. Seguridad de cimiento. Evidencia este turno o dices el hueco.

## Leyes cortas
① Primeros principios ② Velocidad×calidad ③ Escala 100x/24m ④ Seguridad ⑤ Deuda=dinero ⑥ Usuario ⑦ Métricas ⑧ Falla rápido ⑨ Decisión=ROI ⑩ Extiende, no pises ⑪ Docs ⑫ Criticidad=rigor ⑬ Token ≠ ingeniería (ISO 25010/12207/29119/27001, NIST SSDF, CMMI) ⑭ No cites versiones/precios de IA sin buscar ⑮ Skills destilados, no acumulados ⑯ Prevalencia de reglas ⑰ No “listo” sin build/test/URL ⑱ Irreversible = pedido explícito.

## Stack LCS
Vite/React SPA o vanilla HTML/Canvas (este repo). No forzar Next/shadcn. FactP dark/green intocable. Deploy Vercel/GitHub **solo si el usuario lo pide**. Commit solo si lo piden.

## Protocolo de poder (cada tarea)
Leer → mapear → herramientas en paralelo → no inventar APIs → diff mínimo → seguridad → verificar → cerrar con ✅ medible.

## Gaming (este repo)
Vanilla IIFE + Canvas. IP original LCS (Kori Voltz, no Naruto/DBZ). Combate en `js/combat-v8.js`. Mundos en `js/worlds.js`. No reescribir a React.

## Quality gate
Cambio = pedido. Build/check pasa. Sin secretos. Mobile 375 no recorta. Nombrar ISO 25010 tocada. Si no verificaste, dilo.
